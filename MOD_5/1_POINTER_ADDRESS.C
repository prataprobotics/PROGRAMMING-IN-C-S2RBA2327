#include <stdio.h>

int main (){
int count = 100, number=34454;

printf ("the value of count %d \n",count);
printf ("the address of count %p \n",&count);
printf ("the address of number %p \n",&number);
return 0;

}